import { RightNavDirective } from './right-nav.directive';

describe('RightNavDirective', () => {
  it('should create an instance', () => {
    const directive = new RightNavDirective();
    expect(directive).toBeTruthy();
  });
});
